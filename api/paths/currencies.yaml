paths:
  /currencies:
    get:
      summary: List all currencies
      tags: [Currencies]
      security:
        - bearerAuth: []
      responses:
        200:
          description: A list of currencies
          content:
            application/json:
              schema:
                $ref: '../components.yaml#/components/schemas/CurrenciesResponse'
        401:
          $ref: '../components.yaml#/components/responses/Unauthorized'
